# Employee Management (Spring Boot)

Spring Boot REST API generated by [Synth CLI](https://github.com/your-org/synth-cli).

## Requirements

- Java 21+
- Maven 3.9+
- Docker (for integration tests via Testcontainers)

## Quick Start

```bash
# Run the application (uses H2 in-memory DB by default)
mvn spring-boot:run

# Run all tests (requires Docker for Testcontainers)
mvn test

# Format code
mvn spotless:apply
```

## API Endpoints

Base URL: `http://localhost:8080`

### Department — `/api/departments`

| Method | Path | Description | Status |
|--------|------|-------------|--------|
| `GET` | `/api/departments` | List all | 200 |
| `GET` | `/api/departments/{id}` | Find by ID | 200 / 404 |
| `POST` | `/api/departments` | Create | 201 / 400 / 409 |
| `PUT` | `/api/departments/{id}` | Update | 200 / 400 / 404 |
| `DELETE` | `/api/departments/{id}` | Delete | 204 / 404 |

### Employee — `/api/employees`

| Method | Path | Description | Status |
|--------|------|-------------|--------|
| `GET` | `/api/employees` | List all | 200 |
| `GET` | `/api/employees/{id}` | Find by ID | 200 / 404 |
| `POST` | `/api/employees` | Create | 201 / 400 / 409 |
| `PUT` | `/api/employees/{id}` | Update | 200 / 400 / 404 |
| `DELETE` | `/api/employees/{id}` | Delete | 204 / 404 |

### User — `/api/users`

| Method | Path | Description | Status |
|--------|------|-------------|--------|
| `GET` | `/api/users` | List all | 200 |
| `GET` | `/api/users/{id}` | Find by ID | 200 / 404 |
| `POST` | `/api/users` | Create | 201 / 400 / 409 |
| `PUT` | `/api/users/{id}` | Update | 200 / 400 / 404 |
| `DELETE` | `/api/users/{id}` | Delete | 204 / 404 |

### TimeOffRequest — `/api/time-off-requests`

| Method | Path | Description | Status |
|--------|------|-------------|--------|
| `GET` | `/api/time-off-requests` | List all | 200 |
| `GET` | `/api/time-off-requests/{id}` | Find by ID | 200 / 404 |
| `POST` | `/api/time-off-requests` | Create | 201 / 400 / 409 |
| `PUT` | `/api/time-off-requests/{id}` | Update | 200 / 400 / 404 |
| `DELETE` | `/api/time-off-requests/{id}` | Delete | 204 / 404 |

### EmployeeDocument — `/api/employee-documents`

| Method | Path | Description | Status |
|--------|------|-------------|--------|
| `GET` | `/api/employee-documents` | List all | 200 |
| `GET` | `/api/employee-documents/{id}` | Find by ID | 200 / 404 |
| `POST` | `/api/employee-documents` | Create | 201 / 400 / 409 |
| `PUT` | `/api/employee-documents/{id}` | Update | 200 / 400 / 404 |
| `DELETE` | `/api/employee-documents/{id}` | Delete | 204 / 404 |

## OpenAPI / Swagger

| URL | Description |
|-----|-------------|
| `http://localhost:8080/swagger-ui.html` | Interactive Swagger UI |
| `http://localhost:8080/v3/api-docs` | OpenAPI JSON spec |

## Error Schema

All error responses follow [RFC 9457 Problem Details](https://www.rfc-editor.org/rfc/rfc9457):

```json
{
  "type": "about:blank",
  "title": "Validation failed",
  "status": 400,
  "instance": "/api/resource",
  "errors": [
    { "field": "name", "message": "must not be blank" }
  ]
}
```

## Testing

Tests are split into two layers:

**Unit tests** (`*ServiceTest`) — pure service logic, Mockito mocks for repository, no Spring context.

**Integration tests** (`*ControllerIntegrationTest`, `OpenApiSmokeTest`) — full Spring Boot context with a real PostgreSQL instance via [Testcontainers](https://testcontainers.com). Docker must be running.

```bash
# Run only unit tests (no Docker needed)
mvn test -Dgroups="unit"

# Run all tests including integration
mvn test
```
## Message Broker

> **Limitation:** No message broker is configured for this project.
> Docker Compose and related infrastructure files are not generated.
>
> To add broker support, specify a broker in the Technical Context of your spec:
>
> ```
> **Message Broker**: kafka   # or: rabbitmq | redis
> ```
>
> Then re-generate the project. Supported brokers: Kafka, RabbitMQ, Redis.

## Security

Spring Security is enabled. Default in-memory credentials for development:

| Username | Password | Roles |
|----------|----------|-------|
| `admin` | `admin` | ADMIN, USER |
| `user` | `user` | USER |

Replace with your authentication provider (JWT, OAuth2) before deploying to production.
